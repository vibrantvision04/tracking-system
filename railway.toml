[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/vehicles"
healthcheckTimeout = 30

# Tell Railway the HTTP server listens on 8080.
# The GPS TCP server runs on 5027 via the TCP proxy separately.
[[deploy.environmentVariables]]
name = "PORT"
value = "8080"
